// DO NOT MODIFY THE FUNCTION DEFINITIONS

/**
 * Reverse a string, respecting case-sensitivity.
 *
 * @param {string} str
 * @returns {string}
 *
 * @example
 * reverseString("hello");        // => "olleh"
 * reverseString("JavaScript");   // => "tpircSavaJ"
 */
export function reverseString(str) {
  // TODO: complete me
  return null;
}

/**
 * Find the frequency of each character in a string (case-insensitive).
 * Uppercase and lowercase letters are treated as the same character.
 *
 * @param {string} str
 * @returns {Object} An object where keys are lowercase characters and values are their frequencies.
 *
 * @example
 * charFrequency("hello");          // => { h: 1, e: 1, l: 2, o: 1 }
 * charFrequency("WORLD");          // => { w: 1, o: 1, r: 1, l: 1, d: 1 }
 * charFrequency("aabbc");          // => { a: 2, b: 2, c: 1 }
 * charFrequency("AaBbC");          // => { a: 2, b: 2, c: 1 }
 */
export function charFrequency(str) {
  // TODO: complete me
  return null;
}

/**
 * Find the second largest unique number in an array or `undefined`.
 *
 * @param {number[]} nums
 * @returns {number | undefined}
 *
 * @example
 * secondLargestUnique([1,2,3])         // => 2
 * secondLargestUnique([1,2,2,3])       // => 1
 * secondLargestUnique([4,2,7,7,5,2])   // => 4
 * secondLargestUnique([])              // => undefined
 * secondLargestUnique([1])             // => undefined
 * secondLargestUnique([1,1,2,2])       // => undefined
 */
export function secondLargestUnique(nums) {
  // TODO: complete me
  return null;
}